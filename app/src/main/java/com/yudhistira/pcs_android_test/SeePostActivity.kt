package com.yudhistira.pcs_android_test

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

class SeePostActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_see_post)

        //TODO (SeePostActivity)
        // if from API {show post from API}
        // if from SQLite {show post from table posts on DBSqlite }
    }
}