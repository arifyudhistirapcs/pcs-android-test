package com.yudhistira.pcs_android_test.fragments

class ConfigFragment {
    // TODO (ConfigFragment)
    // list menu :
    // 1. Bluetooth (Button to turn on / off bluetooth -> on success make toast)
    // 2. Wifi (Button to turn on / off wifi -> on success make toast)
    // 3. User (on click go to activity to show user profile from DB Sqlite table user !!only show username, name and email!!)
}